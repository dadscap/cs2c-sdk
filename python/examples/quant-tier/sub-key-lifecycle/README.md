# Sub-Key Lifecycle

Demonstrates `GET`, `POST`, `PATCH`, reissue, and `DELETE` operations under `/v1/account/sub-keys`.

```bash
python examples/quant-tier/sub-key-lifecycle/sub_key_lifecycle.py
python examples/quant-tier/sub-key-lifecycle/sub_key_lifecycle.py --keep-resources
```
