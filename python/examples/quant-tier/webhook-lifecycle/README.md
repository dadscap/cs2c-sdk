# Webhook Lifecycle

Demonstrates webhook list, create, update, secret rotation, delivery history, delivery detail, and cleanup under `/v1/account/webhooks`.

```bash
python examples/quant-tier/webhook-lifecycle/webhook_lifecycle.py
python examples/quant-tier/webhook-lifecycle/webhook_lifecycle.py --keep-resources
```
