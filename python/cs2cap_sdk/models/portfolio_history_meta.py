# coding: utf-8

"""
    CS2Cap

    CS2Cap aggregates and analyzes Counter-Strike 2 marketplace data across multiple providers via a unified REST API.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import date
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class PortfolioHistoryMeta(BaseModel):
    """
    Metadata for saved-portfolio historical valuation.
    """ # noqa: E501
    portfolio_id: StrictStr = Field(description="Portfolio UUID.")
    currency: StrictStr = Field(description="Currency code for all money values.")
    providers_queried: List[StrictStr] = Field(description="Provider keys queried.")
    start_date: date = Field(description="Inclusive UTC start date for the requested window.")
    end_date: date = Field(description="Inclusive UTC end date for the requested window.")
    interval: Optional[StrictStr] = Field(default='1d', description="History interval. Always `1d`.")
    holdings_basis: Optional[StrictStr] = Field(default='ledger_close', description="Holdings replay basis. Always `ledger_close`.")
    valuation_basis: Optional[StrictStr] = Field(default='best_provider_close', description="Valuation basis. Always `best_provider_close`.")
    __properties: ClassVar[List[str]] = ["portfolio_id", "currency", "providers_queried", "start_date", "end_date", "interval", "holdings_basis", "valuation_basis"]

    @field_validator('interval')
    def interval_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['1d']):
            raise ValueError("must be one of enum values ('1d')")
        return value

    @field_validator('holdings_basis')
    def holdings_basis_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ledger_close']):
            raise ValueError("must be one of enum values ('ledger_close')")
        return value

    @field_validator('valuation_basis')
    def valuation_basis_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['best_provider_close']):
            raise ValueError("must be one of enum values ('best_provider_close')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PortfolioHistoryMeta from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PortfolioHistoryMeta from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "portfolio_id": obj.get("portfolio_id"),
            "currency": obj.get("currency"),
            "providers_queried": obj.get("providers_queried"),
            "start_date": obj.get("start_date"),
            "end_date": obj.get("end_date"),
            "interval": obj.get("interval") if obj.get("interval") is not None else '1d',
            "holdings_basis": obj.get("holdings_basis") if obj.get("holdings_basis") is not None else 'ledger_close',
            "valuation_basis": obj.get("valuation_basis") if obj.get("valuation_basis") is not None else 'best_provider_close'
        })
        return _obj


