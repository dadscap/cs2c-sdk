/* tslint:disable */
/* eslint-disable */
/**
 * CS2Cap
 * CS2Cap aggregates and analyzes Counter-Strike 2 marketplace data across multiple providers via a unified REST API.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BatchBidsRequest,
  BatchBidsResponse,
  BidsResponse,
  BuyOrderProvider,
  ErrorResponse,
  PhaseName,
  ValidationErrorResponse,
} from '../models/index';
import {
    BatchBidsRequestFromJSON,
    BatchBidsRequestToJSON,
    BatchBidsResponseFromJSON,
    BatchBidsResponseToJSON,
    BidsResponseFromJSON,
    BidsResponseToJSON,
    BuyOrderProviderFromJSON,
    BuyOrderProviderToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    PhaseNameFromJSON,
    PhaseNameToJSON,
    ValidationErrorResponseFromJSON,
    ValidationErrorResponseToJSON,
} from '../models/index';

export interface BatchBidLookupRequest {
    batchBidsRequest: BatchBidsRequest;
}

export interface ListBidsRequest {
    itemId?: number | null;
    marketHashName?: string | null;
    phase?: PhaseName | null;
    providers?: Array<BuyOrderProvider> | null;
    currency?: string;
    limit?: number;
    offset?: number;
}

export interface StreamFullBidsSnapshotRequest {
    providers?: Array<BuyOrderProvider> | null;
}

/**
 * 
 */
export class BidsApi extends runtime.BaseAPI {

    /**
     * Creates request options for batchBidLookup without sending the request
     */
    async batchBidLookupRequestOpts(requestParameters: BatchBidLookupRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['batchBidsRequest'] == null) {
            throw new runtime.RequiredError(
                'batchBidsRequest',
                'Required parameter "batchBidsRequest" was null or undefined when calling batchBidLookup().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/bids/batch`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BatchBidsRequestToJSON(requestParameters['batchBidsRequest']),
        };
    }

    /**
     * Retrieve current bids (buy orders) for multiple items in a single request.  Maximum 100 items per request. Returns per-item bid quotes from each queried provider.  Prices are in minor units of the requested currency (e.g. USD cents when `currency=USD`). Divide by 100 for display.
     * Batch Bid Lookup
     */
    async batchBidLookupRaw(requestParameters: BatchBidLookupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchBidsResponse>> {
        const requestOptions = await this.batchBidLookupRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BatchBidsResponseFromJSON(jsonValue));
    }

    /**
     * Retrieve current bids (buy orders) for multiple items in a single request.  Maximum 100 items per request. Returns per-item bid quotes from each queried provider.  Prices are in minor units of the requested currency (e.g. USD cents when `currency=USD`). Divide by 100 for display.
     * Batch Bid Lookup
     */
    async batchBidLookup(requestParameters: BatchBidLookupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchBidsResponse> {
        const response = await this.batchBidLookupRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listBids without sending the request
     */
    async listBidsRequestOpts(requestParameters: ListBidsRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['itemId'] != null) {
            queryParameters['item_id'] = requestParameters['itemId'];
        }

        if (requestParameters['marketHashName'] != null) {
            queryParameters['market_hash_name'] = requestParameters['marketHashName'];
        }

        if (requestParameters['phase'] != null) {
            queryParameters['phase'] = requestParameters['phase'];
        }

        if (requestParameters['providers'] != null) {
            queryParameters['providers'] = requestParameters['providers'];
        }

        if (requestParameters['currency'] != null) {
            queryParameters['currency'] = requestParameters['currency'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/bids`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Return current highest bids from providers that support buy orders.  Filters: - `item_id` or `market_hash_name` - optional `phase` - `providers` limited to buy-order-capable provider keys - `currency`, `limit`, and `offset`  Behavior: - broad listings use the indexed pagination path - item-specific requests use direct indexed lookup - unsupported providers return `400`  Response: - `meta` with filters and providers queried - flattened per-provider bid rows - offset pagination metadata
     * List Bids
     */
    async listBidsRaw(requestParameters: ListBidsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BidsResponse>> {
        const requestOptions = await this.listBidsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BidsResponseFromJSON(jsonValue));
    }

    /**
     * Return current highest bids from providers that support buy orders.  Filters: - `item_id` or `market_hash_name` - optional `phase` - `providers` limited to buy-order-capable provider keys - `currency`, `limit`, and `offset`  Behavior: - broad listings use the indexed pagination path - item-specific requests use direct indexed lookup - unsupported providers return `400`  Response: - `meta` with filters and providers queried - flattened per-provider bid rows - offset pagination metadata
     * List Bids
     */
    async listBids(requestParameters: ListBidsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BidsResponse> {
        const response = await this.listBidsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for streamFullBidsSnapshot without sending the request
     */
    async streamFullBidsSnapshotRequestOpts(requestParameters: StreamFullBidsSnapshotRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['providers'] != null) {
            queryParameters['providers'] = requestParameters['providers'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/bids`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Return the full live bids snapshot as an NDJSON stream.  Behavior: - pro and quant tiers only - requires a real `sk_*` API key; session JWTs are not accepted - optional `providers` filter; omit to stream all providers - fixed USD output - `highest_bid` values are returned in USD minor units - one JSON object per line using the `BuyOrderItem` field set - the live index is copied into temporary Redis keys so the export is stable for the duration of the stream - per-API-key cooldown of 30 seconds for this POST operation
     * Stream Full Bids Snapshot
     */
    async streamFullBidsSnapshotRaw(requestParameters: StreamFullBidsSnapshotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<string>> {
        const requestOptions = await this.streamFullBidsSnapshotRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<string>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Return the full live bids snapshot as an NDJSON stream.  Behavior: - pro and quant tiers only - requires a real `sk_*` API key; session JWTs are not accepted - optional `providers` filter; omit to stream all providers - fixed USD output - `highest_bid` values are returned in USD minor units - one JSON object per line using the `BuyOrderItem` field set - the live index is copied into temporary Redis keys so the export is stable for the duration of the stream - per-API-key cooldown of 30 seconds for this POST operation
     * Stream Full Bids Snapshot
     */
    async streamFullBidsSnapshot(requestParameters: StreamFullBidsSnapshotRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<string> {
        const response = await this.streamFullBidsSnapshotRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
