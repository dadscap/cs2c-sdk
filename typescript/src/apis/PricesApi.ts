/* tslint:disable */
/* eslint-disable */
/**
 * CS2C-API
 * CS2C-API aggregates and analyzes Counter-Strike 2 marketplace data across multiple providers via a unified REST API.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: dadscaptv@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AllProviders,
  ErrorResponse,
  PhaseName,
  PriceCandlesPage,
  PriceSnapshotPage,
  PricesPaginatedResponseMarketItem,
  ValidationErrorResponse,
} from '../models/index';
import {
    AllProvidersFromJSON,
    AllProvidersToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    PhaseNameFromJSON,
    PhaseNameToJSON,
    PriceCandlesPageFromJSON,
    PriceCandlesPageToJSON,
    PriceSnapshotPageFromJSON,
    PriceSnapshotPageToJSON,
    PricesPaginatedResponseMarketItemFromJSON,
    PricesPaginatedResponseMarketItemToJSON,
    ValidationErrorResponseFromJSON,
    ValidationErrorResponseToJSON,
} from '../models/index';

export interface ListPricesV1PricesGetRequest {
    itemId?: number | null;
    marketHashName?: string | null;
    phase?: PhaseName | null;
    providers?: Array<AllProviders> | null;
    currency?: string;
    limit?: number | null;
    offset?: number;
}

export interface PriceCandlesV1PricesCandlesGetRequest {
    provider: string;
    itemId?: number | null;
    marketHashName?: string | null;
    phase?: PhaseName | null;
    start?: string | null;
    end?: string | null;
    lookback?: string | null;
    interval?: PriceCandlesV1PricesCandlesGetIntervalEnum;
    fill?: boolean;
    currency?: string;
    limit?: number | null;
    cursor?: string | null;
}

export interface PriceHistoryV1PricesHistoryGetRequest {
    itemId?: number | null;
    marketHashName?: string | null;
    phase?: PhaseName | null;
    provider?: string | null;
    start?: string | null;
    end?: string | null;
    currency?: string;
    limit?: number | null;
    cursor?: string | null;
}

/**
 * 
 */
export class PricesApi extends runtime.BaseAPI {

    /**
     * Creates request options for listPricesV1PricesGet without sending the request
     */
    async listPricesV1PricesGetRequestOpts(requestParameters: ListPricesV1PricesGetRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['itemId'] != null) {
            queryParameters['item_id'] = requestParameters['itemId'];
        }

        if (requestParameters['marketHashName'] != null) {
            queryParameters['market_hash_name'] = requestParameters['marketHashName'];
        }

        if (requestParameters['phase'] != null) {
            queryParameters['phase'] = requestParameters['phase'];
        }

        if (requestParameters['providers'] != null) {
            queryParameters['providers'] = requestParameters['providers'];
        }

        if (requestParameters['currency'] != null) {
            queryParameters['currency'] = requestParameters['currency'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/prices`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Return current lowest ask listings.  Parameters: - `item_id`: Exact item ID (recommended when known). - `market_hash_name`: Exact market name. - `phase`: Optional Doppler phase filter. - `providers`: Optional provider-key enum list (repeat `providers` to pass multiple values). - `currency`: Quote currency for prices (default `USD`). - `limit`, `offset`: Offset pagination controls.  Behavior: - If no filters are provided, returns a broad paginated listing. - If `market_hash_name` is Doppler and `phase` is omitted, results aggregate across phases per provider. - If `market_hash_name` and `phase` are both provided, only that exact phase is returned.  Response: - `meta`: Applied filters, requested providers, returned providers, currency. - `items`: Per-provider lowest ask entries. - `pagination`: `limit`, `offset`, `total`, `has_next`, `has_prev`.  Requirements: - Valid API key with access to `/v1/prices`. - `limit` is capped by your tier.
     * List Prices
     */
    async listPricesV1PricesGetRaw(requestParameters: ListPricesV1PricesGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricesPaginatedResponseMarketItem>> {
        const requestOptions = await this.listPricesV1PricesGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricesPaginatedResponseMarketItemFromJSON(jsonValue));
    }

    /**
     * Return current lowest ask listings.  Parameters: - `item_id`: Exact item ID (recommended when known). - `market_hash_name`: Exact market name. - `phase`: Optional Doppler phase filter. - `providers`: Optional provider-key enum list (repeat `providers` to pass multiple values). - `currency`: Quote currency for prices (default `USD`). - `limit`, `offset`: Offset pagination controls.  Behavior: - If no filters are provided, returns a broad paginated listing. - If `market_hash_name` is Doppler and `phase` is omitted, results aggregate across phases per provider. - If `market_hash_name` and `phase` are both provided, only that exact phase is returned.  Response: - `meta`: Applied filters, requested providers, returned providers, currency. - `items`: Per-provider lowest ask entries. - `pagination`: `limit`, `offset`, `total`, `has_next`, `has_prev`.  Requirements: - Valid API key with access to `/v1/prices`. - `limit` is capped by your tier.
     * List Prices
     */
    async listPricesV1PricesGet(requestParameters: ListPricesV1PricesGetRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricesPaginatedResponseMarketItem> {
        const response = await this.listPricesV1PricesGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for priceCandlesV1PricesCandlesGet without sending the request
     */
    async priceCandlesV1PricesCandlesGetRequestOpts(requestParameters: PriceCandlesV1PricesCandlesGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['provider'] == null) {
            throw new runtime.RequiredError(
                'provider',
                'Required parameter "provider" was null or undefined when calling priceCandlesV1PricesCandlesGet().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['itemId'] != null) {
            queryParameters['item_id'] = requestParameters['itemId'];
        }

        if (requestParameters['marketHashName'] != null) {
            queryParameters['market_hash_name'] = requestParameters['marketHashName'];
        }

        if (requestParameters['phase'] != null) {
            queryParameters['phase'] = requestParameters['phase'];
        }

        if (requestParameters['provider'] != null) {
            queryParameters['provider'] = requestParameters['provider'];
        }

        if (requestParameters['start'] != null) {
            queryParameters['start'] = requestParameters['start'];
        }

        if (requestParameters['end'] != null) {
            queryParameters['end'] = requestParameters['end'];
        }

        if (requestParameters['lookback'] != null) {
            queryParameters['lookback'] = requestParameters['lookback'];
        }

        if (requestParameters['interval'] != null) {
            queryParameters['interval'] = requestParameters['interval'];
        }

        if (requestParameters['fill'] != null) {
            queryParameters['fill'] = requestParameters['fill'];
        }

        if (requestParameters['currency'] != null) {
            queryParameters['currency'] = requestParameters['currency'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/prices/candles`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Return OHLCV candle data for a single item and single provider.  Parameters: - Item filter: `item_id` or `market_hash_name` (one is required). - Optional filter: `phase`. - Required filter: `provider` (singular string key for one specific provider). - Time window: `start`/`end` (ISO 8601) or `lookback` (for example `7d`, `30d`). - `interval`: `5m`, `1h`, or `1d`. - `fill`: Include empty buckets with carry-forward prices when `true`. - `currency`: Quote currency (default `USD`). - `limit`, `cursor`: Cursor-based pagination controls.  Notes: - If both `start` and `lookback` are sent, `lookback` takes precedence. - Maximum lookback depends on `interval`:   `5m` up to 7 days, `1h` up to 30 days, `1d` up to 365 days.  Response: - `meta`: Item, interval, provider/phase, currency, start/end. - `data`: Candle buckets in oldest-to-newest order (`t`, `o`, `h`, `l`, `c`, `v`).   `v` is the last observed listing quantity in the bucket, not transaction volume. - `pagination`: Standardized pagination footer with cursor state.  Requirements: - Valid API key with access to `/v1/prices/candles`. - `item_id` or `market_hash_name` is required. - `provider` is required and accepts exactly one provider key. - `start`/`end` must be valid ISO 8601 timestamps. - `limit` is capped by your tier.
     * Price Candles
     */
    async priceCandlesV1PricesCandlesGetRaw(requestParameters: PriceCandlesV1PricesCandlesGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PriceCandlesPage>> {
        const requestOptions = await this.priceCandlesV1PricesCandlesGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PriceCandlesPageFromJSON(jsonValue));
    }

    /**
     * Return OHLCV candle data for a single item and single provider.  Parameters: - Item filter: `item_id` or `market_hash_name` (one is required). - Optional filter: `phase`. - Required filter: `provider` (singular string key for one specific provider). - Time window: `start`/`end` (ISO 8601) or `lookback` (for example `7d`, `30d`). - `interval`: `5m`, `1h`, or `1d`. - `fill`: Include empty buckets with carry-forward prices when `true`. - `currency`: Quote currency (default `USD`). - `limit`, `cursor`: Cursor-based pagination controls.  Notes: - If both `start` and `lookback` are sent, `lookback` takes precedence. - Maximum lookback depends on `interval`:   `5m` up to 7 days, `1h` up to 30 days, `1d` up to 365 days.  Response: - `meta`: Item, interval, provider/phase, currency, start/end. - `data`: Candle buckets in oldest-to-newest order (`t`, `o`, `h`, `l`, `c`, `v`).   `v` is the last observed listing quantity in the bucket, not transaction volume. - `pagination`: Standardized pagination footer with cursor state.  Requirements: - Valid API key with access to `/v1/prices/candles`. - `item_id` or `market_hash_name` is required. - `provider` is required and accepts exactly one provider key. - `start`/`end` must be valid ISO 8601 timestamps. - `limit` is capped by your tier.
     * Price Candles
     */
    async priceCandlesV1PricesCandlesGet(requestParameters: PriceCandlesV1PricesCandlesGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PriceCandlesPage> {
        const response = await this.priceCandlesV1PricesCandlesGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for priceHistoryV1PricesHistoryGet without sending the request
     */
    async priceHistoryV1PricesHistoryGetRequestOpts(requestParameters: PriceHistoryV1PricesHistoryGetRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['itemId'] != null) {
            queryParameters['item_id'] = requestParameters['itemId'];
        }

        if (requestParameters['marketHashName'] != null) {
            queryParameters['market_hash_name'] = requestParameters['marketHashName'];
        }

        if (requestParameters['phase'] != null) {
            queryParameters['phase'] = requestParameters['phase'];
        }

        if (requestParameters['provider'] != null) {
            queryParameters['provider'] = requestParameters['provider'];
        }

        if (requestParameters['start'] != null) {
            queryParameters['start'] = requestParameters['start'];
        }

        if (requestParameters['end'] != null) {
            queryParameters['end'] = requestParameters['end'];
        }

        if (requestParameters['currency'] != null) {
            queryParameters['currency'] = requestParameters['currency'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/prices/history`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Return historical price snapshots.  Parameters: - Filters: `item_id`, `market_hash_name`, `phase`, `provider` (singular string key). - Time window: `start`, `end` (ISO 8601). - `currency`: Quote currency (default `USD`). - `limit`: Items per page. - `cursor`: Opaque cursor from `next_cursor` for the next page.  Response: - `items`: Historical snapshots ordered newest first. - `meta`: Applied filters and time window. - `pagination`: Standardized pagination footer with cursor state.  Requirements: - Valid API key with access to `/v1/prices/history`. - `start`/`end` must be valid ISO 8601 timestamps. - Requested time range must be within the endpoint retention window. - `limit` is capped by your tier.
     * Price History
     */
    async priceHistoryV1PricesHistoryGetRaw(requestParameters: PriceHistoryV1PricesHistoryGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PriceSnapshotPage>> {
        const requestOptions = await this.priceHistoryV1PricesHistoryGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PriceSnapshotPageFromJSON(jsonValue));
    }

    /**
     * Return historical price snapshots.  Parameters: - Filters: `item_id`, `market_hash_name`, `phase`, `provider` (singular string key). - Time window: `start`, `end` (ISO 8601). - `currency`: Quote currency (default `USD`). - `limit`: Items per page. - `cursor`: Opaque cursor from `next_cursor` for the next page.  Response: - `items`: Historical snapshots ordered newest first. - `meta`: Applied filters and time window. - `pagination`: Standardized pagination footer with cursor state.  Requirements: - Valid API key with access to `/v1/prices/history`. - `start`/`end` must be valid ISO 8601 timestamps. - Requested time range must be within the endpoint retention window. - `limit` is capped by your tier.
     * Price History
     */
    async priceHistoryV1PricesHistoryGet(requestParameters: PriceHistoryV1PricesHistoryGetRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PriceSnapshotPage> {
        const response = await this.priceHistoryV1PricesHistoryGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const PriceCandlesV1PricesCandlesGetIntervalEnum = {
    _1d: '1d',
    _1h: '1h',
    _5m: '5m'
} as const;
export type PriceCandlesV1PricesCandlesGetIntervalEnum = typeof PriceCandlesV1PricesCandlesGetIntervalEnum[keyof typeof PriceCandlesV1PricesCandlesGetIntervalEnum];
