/* tslint:disable */
/* eslint-disable */
/**
 * CS2Cap
 * CS2Cap aggregates and analyzes Counter-Strike 2 marketplace data across multiple providers via a unified REST API.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AllProviders,
  BatchPricesRequest,
  BatchPricesResponse,
  ErrorResponse,
  PhaseName,
  PriceCandlesPage,
  PriceSnapshotPage,
  PricesPaginatedResponseMarketItem,
  ValidationErrorResponse,
} from '../models/index';
import {
    AllProvidersFromJSON,
    AllProvidersToJSON,
    BatchPricesRequestFromJSON,
    BatchPricesRequestToJSON,
    BatchPricesResponseFromJSON,
    BatchPricesResponseToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    PhaseNameFromJSON,
    PhaseNameToJSON,
    PriceCandlesPageFromJSON,
    PriceCandlesPageToJSON,
    PriceSnapshotPageFromJSON,
    PriceSnapshotPageToJSON,
    PricesPaginatedResponseMarketItemFromJSON,
    PricesPaginatedResponseMarketItemToJSON,
    ValidationErrorResponseFromJSON,
    ValidationErrorResponseToJSON,
} from '../models/index';

export interface BatchPriceLookupRequest {
    batchPricesRequest: BatchPricesRequest;
}

export interface ListPricesRequest {
    itemId?: number | null;
    marketHashName?: string | null;
    phase?: PhaseName | null;
    providers?: Array<AllProviders> | null;
    currency?: string;
    limit?: number | null;
    offset?: number;
}

export interface PriceCandlesRequest {
    itemId?: number | null;
    marketHashName?: string | null;
    phase?: PhaseName | null;
    start?: Date | null;
    end?: Date | null;
    lookback?: string | null;
    interval?: PriceCandlesIntervalEnum;
    fill?: boolean;
    currency?: string;
}

export interface PriceHistoryRequest {
    itemId?: number | null;
    marketHashName?: string | null;
    phase?: PhaseName | null;
    provider?: AllProviders | null;
    start?: Date | null;
    end?: Date | null;
    currency?: string;
    limit?: number | null;
    cursor?: string | null;
}

export interface StreamFullPricesSnapshotRequest {
    providers?: Array<AllProviders> | null;
}

/**
 * 
 */
export class PricesApi extends runtime.BaseAPI {

    /**
     * Creates request options for batchPriceLookup without sending the request
     */
    async batchPriceLookupRequestOpts(requestParameters: BatchPriceLookupRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['batchPricesRequest'] == null) {
            throw new runtime.RequiredError(
                'batchPricesRequest',
                'Required parameter "batchPricesRequest" was null or undefined when calling batchPriceLookup().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/prices/batch`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BatchPricesRequestToJSON(requestParameters['batchPricesRequest']),
        };
    }

    /**
     * Retrieve current prices for multiple items in a single request.  Maximum 100 items per request. Returns per-item price quotes from each queried provider.  Prices are in minor units of the requested currency (e.g. USD cents when `currency=USD`). Divide by 100 for display.
     * Batch Price Lookup
     */
    async batchPriceLookupRaw(requestParameters: BatchPriceLookupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchPricesResponse>> {
        const requestOptions = await this.batchPriceLookupRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BatchPricesResponseFromJSON(jsonValue));
    }

    /**
     * Retrieve current prices for multiple items in a single request.  Maximum 100 items per request. Returns per-item price quotes from each queried provider.  Prices are in minor units of the requested currency (e.g. USD cents when `currency=USD`). Divide by 100 for display.
     * Batch Price Lookup
     */
    async batchPriceLookup(requestParameters: BatchPriceLookupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchPricesResponse> {
        const response = await this.batchPriceLookupRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listPrices without sending the request
     */
    async listPricesRequestOpts(requestParameters: ListPricesRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['itemId'] != null) {
            queryParameters['item_id'] = requestParameters['itemId'];
        }

        if (requestParameters['marketHashName'] != null) {
            queryParameters['market_hash_name'] = requestParameters['marketHashName'];
        }

        if (requestParameters['phase'] != null) {
            queryParameters['phase'] = requestParameters['phase'];
        }

        if (requestParameters['providers'] != null) {
            queryParameters['providers'] = requestParameters['providers'];
        }

        if (requestParameters['currency'] != null) {
            queryParameters['currency'] = requestParameters['currency'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/prices`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Return current lowest ask listings.  Parameters: - `item_id`: Exact item ID (recommended when known). - `market_hash_name`: Exact market name. - `phase`: Optional Doppler phase filter. - `providers`: Optional provider-key enum list (repeat `providers` to pass multiple values). - `currency`: Quote currency for prices (default `USD`). - `limit`, `offset`: Offset pagination controls.  Behavior: - If no filters are provided, returns a broad paginated listing. - If `market_hash_name` is Doppler and `phase` is omitted, results aggregate across phases per provider. - If `market_hash_name` and `phase` are both provided, only that exact phase is returned. - If the item resolves in the catalog but none of the queried providers currently has a   listing, returns `200` with an empty `items` array.  Response: - `meta`: Applied filters, requested providers, returned providers, currency. - `items`: Per-provider lowest ask entries. - `pagination`: `limit`, `offset`, `total`, `has_next`, `has_prev`.  Requirements: - Valid API key with access to `/v1/prices`. - `limit` is capped by your tier.
     * List Prices
     */
    async listPricesRaw(requestParameters: ListPricesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PricesPaginatedResponseMarketItem>> {
        const requestOptions = await this.listPricesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PricesPaginatedResponseMarketItemFromJSON(jsonValue));
    }

    /**
     * Return current lowest ask listings.  Parameters: - `item_id`: Exact item ID (recommended when known). - `market_hash_name`: Exact market name. - `phase`: Optional Doppler phase filter. - `providers`: Optional provider-key enum list (repeat `providers` to pass multiple values). - `currency`: Quote currency for prices (default `USD`). - `limit`, `offset`: Offset pagination controls.  Behavior: - If no filters are provided, returns a broad paginated listing. - If `market_hash_name` is Doppler and `phase` is omitted, results aggregate across phases per provider. - If `market_hash_name` and `phase` are both provided, only that exact phase is returned. - If the item resolves in the catalog but none of the queried providers currently has a   listing, returns `200` with an empty `items` array.  Response: - `meta`: Applied filters, requested providers, returned providers, currency. - `items`: Per-provider lowest ask entries. - `pagination`: `limit`, `offset`, `total`, `has_next`, `has_prev`.  Requirements: - Valid API key with access to `/v1/prices`. - `limit` is capped by your tier.
     * List Prices
     */
    async listPrices(requestParameters: ListPricesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PricesPaginatedResponseMarketItem> {
        const response = await this.listPricesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for priceCandles without sending the request
     */
    async priceCandlesRequestOpts(requestParameters: PriceCandlesRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['itemId'] != null) {
            queryParameters['item_id'] = requestParameters['itemId'];
        }

        if (requestParameters['marketHashName'] != null) {
            queryParameters['market_hash_name'] = requestParameters['marketHashName'];
        }

        if (requestParameters['phase'] != null) {
            queryParameters['phase'] = requestParameters['phase'];
        }

        if (requestParameters['start'] != null) {
            queryParameters['start'] = (requestParameters['start'] as any).toISOString();
        }

        if (requestParameters['end'] != null) {
            queryParameters['end'] = (requestParameters['end'] as any).toISOString();
        }

        if (requestParameters['lookback'] != null) {
            queryParameters['lookback'] = requestParameters['lookback'];
        }

        if (requestParameters['interval'] != null) {
            queryParameters['interval'] = requestParameters['interval'];
        }

        if (requestParameters['fill'] != null) {
            queryParameters['fill'] = requestParameters['fill'];
        }

        if (requestParameters['currency'] != null) {
            queryParameters['currency'] = requestParameters['currency'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/prices/candles`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Return composite OHLCV candle data for a single item across all providers.  Parameters: - Item filter: `item_id` or `market_hash_name` (one is required). - Optional filter: `phase`. - Time window: `start`/`end` (ISO 8601) or `lookback` (for example `7d` or `7`). - `interval`: `5m`, `1h`, or `1d`. - `fill`: Include empty buckets with carry-forward prices when `true`. - `currency`: Quote currency (default `USD`).  Notes: - If both `start` and `lookback` are sent, `lookback` takes precedence. - Maximum lookback depends on `interval`:   `5m` up to 7 days, `1h` up to 30 days, `1d` up to 365 days. - Free tiers are additionally capped by their tier restrictions and default   to the maximum allowed window when no explicit lookback is supplied.  Response: - `meta`: Item, provider scope, interval, phase, currency, start/end timestamps. - `data`: Candle buckets in oldest-to-newest order   (`t`, `o`, `h`, `l`, `c`, `v`, `q`, `providers`).   `o`/`c` are the lowest effective provider prices, `l` is the minimum provider   low, `h` is capped at `median(provider_highs) * 1.5`, `v` is the non-negative depletion   flow between buckets, and `q` is the summed close-side inventory at bucket end when   available. `providers.l` and `providers.h` identify the provider keys contributing the   returned low/high values. For `1d` requests starting more than 30 days back, `v` falls   back to legacy depletion-derived `volume_qty` and `q` is `null`.  Requirements: - Valid API key with access to `/v1/prices/candles`. - `item_id` or `market_hash_name` is required. - `start`/`end` must be valid ISO 8601 timestamps.
     * Price Candles
     */
    async priceCandlesRaw(requestParameters: PriceCandlesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PriceCandlesPage>> {
        const requestOptions = await this.priceCandlesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PriceCandlesPageFromJSON(jsonValue));
    }

    /**
     * Return composite OHLCV candle data for a single item across all providers.  Parameters: - Item filter: `item_id` or `market_hash_name` (one is required). - Optional filter: `phase`. - Time window: `start`/`end` (ISO 8601) or `lookback` (for example `7d` or `7`). - `interval`: `5m`, `1h`, or `1d`. - `fill`: Include empty buckets with carry-forward prices when `true`. - `currency`: Quote currency (default `USD`).  Notes: - If both `start` and `lookback` are sent, `lookback` takes precedence. - Maximum lookback depends on `interval`:   `5m` up to 7 days, `1h` up to 30 days, `1d` up to 365 days. - Free tiers are additionally capped by their tier restrictions and default   to the maximum allowed window when no explicit lookback is supplied.  Response: - `meta`: Item, provider scope, interval, phase, currency, start/end timestamps. - `data`: Candle buckets in oldest-to-newest order   (`t`, `o`, `h`, `l`, `c`, `v`, `q`, `providers`).   `o`/`c` are the lowest effective provider prices, `l` is the minimum provider   low, `h` is capped at `median(provider_highs) * 1.5`, `v` is the non-negative depletion   flow between buckets, and `q` is the summed close-side inventory at bucket end when   available. `providers.l` and `providers.h` identify the provider keys contributing the   returned low/high values. For `1d` requests starting more than 30 days back, `v` falls   back to legacy depletion-derived `volume_qty` and `q` is `null`.  Requirements: - Valid API key with access to `/v1/prices/candles`. - `item_id` or `market_hash_name` is required. - `start`/`end` must be valid ISO 8601 timestamps.
     * Price Candles
     */
    async priceCandles(requestParameters: PriceCandlesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PriceCandlesPage> {
        const response = await this.priceCandlesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for priceHistory without sending the request
     */
    async priceHistoryRequestOpts(requestParameters: PriceHistoryRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['itemId'] != null) {
            queryParameters['item_id'] = requestParameters['itemId'];
        }

        if (requestParameters['marketHashName'] != null) {
            queryParameters['market_hash_name'] = requestParameters['marketHashName'];
        }

        if (requestParameters['phase'] != null) {
            queryParameters['phase'] = requestParameters['phase'];
        }

        if (requestParameters['provider'] != null) {
            queryParameters['provider'] = requestParameters['provider'];
        }

        if (requestParameters['start'] != null) {
            queryParameters['start'] = (requestParameters['start'] as any).toISOString();
        }

        if (requestParameters['end'] != null) {
            queryParameters['end'] = (requestParameters['end'] as any).toISOString();
        }

        if (requestParameters['currency'] != null) {
            queryParameters['currency'] = requestParameters['currency'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/prices/history`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Return historical price snapshots.  Parameters: - Filters: `item_id`, `market_hash_name`, `phase`, `provider` (singular string key). - Time window: `start`, `end` (ISO 8601). If `start` is omitted, the   endpoint defaults to the last 14 days of raw history. - `currency`: Quote currency (default `USD`). - `limit`: Items per page. - `cursor`: Opaque cursor from `next_cursor` for the next page.  Response: - `items`: Historical snapshots ordered newest first. - `meta`: Applied filters and time window. - `pagination`: Standardized pagination footer with cursor state.  Requirements: - Valid API key with access to `/v1/prices/history`. - `start`/`end` must be valid ISO 8601 timestamps. - Requested time range must be within the endpoint retention window. - `limit` is capped by your tier.
     * Price History
     */
    async priceHistoryRaw(requestParameters: PriceHistoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PriceSnapshotPage>> {
        const requestOptions = await this.priceHistoryRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PriceSnapshotPageFromJSON(jsonValue));
    }

    /**
     * Return historical price snapshots.  Parameters: - Filters: `item_id`, `market_hash_name`, `phase`, `provider` (singular string key). - Time window: `start`, `end` (ISO 8601). If `start` is omitted, the   endpoint defaults to the last 14 days of raw history. - `currency`: Quote currency (default `USD`). - `limit`: Items per page. - `cursor`: Opaque cursor from `next_cursor` for the next page.  Response: - `items`: Historical snapshots ordered newest first. - `meta`: Applied filters and time window. - `pagination`: Standardized pagination footer with cursor state.  Requirements: - Valid API key with access to `/v1/prices/history`. - `start`/`end` must be valid ISO 8601 timestamps. - Requested time range must be within the endpoint retention window. - `limit` is capped by your tier.
     * Price History
     */
    async priceHistory(requestParameters: PriceHistoryRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PriceSnapshotPage> {
        const response = await this.priceHistoryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for streamFullPricesSnapshot without sending the request
     */
    async streamFullPricesSnapshotRequestOpts(requestParameters: StreamFullPricesSnapshotRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['providers'] != null) {
            queryParameters['providers'] = requestParameters['providers'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/prices`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Return the full live prices snapshot as an NDJSON stream.  Behavior: - pro and quant tiers only - requires a real `sk_*` API key; session JWTs are not accepted - optional `providers` filter; omit to stream all providers - fixed USD output - `lowest_ask` values are returned in USD minor units - one JSON object per line using the `MarketItem` field set - the live index is copied into temporary Redis keys so the export is stable for the duration of the stream - per-API-key cooldown of 30 seconds for this POST operation
     * Stream Full Prices Snapshot
     */
    async streamFullPricesSnapshotRaw(requestParameters: StreamFullPricesSnapshotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<string>> {
        const requestOptions = await this.streamFullPricesSnapshotRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<string>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Return the full live prices snapshot as an NDJSON stream.  Behavior: - pro and quant tiers only - requires a real `sk_*` API key; session JWTs are not accepted - optional `providers` filter; omit to stream all providers - fixed USD output - `lowest_ask` values are returned in USD minor units - one JSON object per line using the `MarketItem` field set - the live index is copied into temporary Redis keys so the export is stable for the duration of the stream - per-API-key cooldown of 30 seconds for this POST operation
     * Stream Full Prices Snapshot
     */
    async streamFullPricesSnapshot(requestParameters: StreamFullPricesSnapshotRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<string> {
        const response = await this.streamFullPricesSnapshotRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const PriceCandlesIntervalEnum = {
    _1d: '1d',
    _1h: '1h',
    _5m: '5m'
} as const;
export type PriceCandlesIntervalEnum = typeof PriceCandlesIntervalEnum[keyof typeof PriceCandlesIntervalEnum];
