/* tslint:disable */
/* eslint-disable */
/**
 * CS2Cap
 * CS2Cap aggregates and analyzes Counter-Strike 2 marketplace data across multiple providers via a unified REST API.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AccountMutationResponse,
  ErrorResponse,
  ValidationErrorResponse,
  WatchlistCreateRequest,
  WatchlistCreateResponse,
  WatchlistResponse,
} from '../models/index';
import {
    AccountMutationResponseFromJSON,
    AccountMutationResponseToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    ValidationErrorResponseFromJSON,
    ValidationErrorResponseToJSON,
    WatchlistCreateRequestFromJSON,
    WatchlistCreateRequestToJSON,
    WatchlistCreateResponseFromJSON,
    WatchlistCreateResponseToJSON,
    WatchlistResponseFromJSON,
    WatchlistResponseToJSON,
} from '../models/index';

export interface CreateWatchlistEntriesRequest {
    watchlistCreateRequest: WatchlistCreateRequest;
}

export interface DeleteWatchlistEntryRequest {
    itemId: number;
}

export interface ListWatchlistRequest {
    limit?: number;
    offset?: number;
    search?: string | null;
}

/**
 * 
 */
export class AccountWatchlistApi extends runtime.BaseAPI {

    /**
     * Creates request options for createWatchlistEntries without sending the request
     */
    async createWatchlistEntriesRequestOpts(requestParameters: CreateWatchlistEntriesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['watchlistCreateRequest'] == null) {
            throw new runtime.RequiredError(
                'watchlistCreateRequest',
                'Required parameter "watchlistCreateRequest" was null or undefined when calling createWatchlistEntries().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/watchlist`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: WatchlistCreateRequestToJSON(requestParameters['watchlistCreateRequest']),
        };
    }

    /**
     * Save one or more normalized catalog items to the authenticated user\'s default watchlist.  Rules: - accepts either a real API key or a session JWT bearer token - send either `item_id` for a single add or `item_ids` for a batch add - every item ID must exist in the API item catalog - watchlist access and max saved items depend on the user\'s tier - batch creates are all-or-nothing - duplicate saves return `409`
     * Create Watchlist Entries
     */
    async createWatchlistEntriesRaw(requestParameters: CreateWatchlistEntriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WatchlistCreateResponse>> {
        const requestOptions = await this.createWatchlistEntriesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WatchlistCreateResponseFromJSON(jsonValue));
    }

    /**
     * Save one or more normalized catalog items to the authenticated user\'s default watchlist.  Rules: - accepts either a real API key or a session JWT bearer token - send either `item_id` for a single add or `item_ids` for a batch add - every item ID must exist in the API item catalog - watchlist access and max saved items depend on the user\'s tier - batch creates are all-or-nothing - duplicate saves return `409`
     * Create Watchlist Entries
     */
    async createWatchlistEntries(requestParameters: CreateWatchlistEntriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WatchlistCreateResponse> {
        const response = await this.createWatchlistEntriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteWatchlistEntry without sending the request
     */
    async deleteWatchlistEntryRequestOpts(requestParameters: DeleteWatchlistEntryRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['itemId'] == null) {
            throw new runtime.RequiredError(
                'itemId',
                'Required parameter "itemId" was null or undefined when calling deleteWatchlistEntry().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/watchlist/{item_id}`;
        urlPath = urlPath.replace(`{${"item_id"}}`, encodeURIComponent(String(requestParameters['itemId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Remove one saved watchlist entry by normalized `item_id`.  This path uses the catalog item ID, not the watchlist entry UUID.
     * Delete Watchlist Entry
     */
    async deleteWatchlistEntryRaw(requestParameters: DeleteWatchlistEntryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountMutationResponse>> {
        const requestOptions = await this.deleteWatchlistEntryRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountMutationResponseFromJSON(jsonValue));
    }

    /**
     * Remove one saved watchlist entry by normalized `item_id`.  This path uses the catalog item ID, not the watchlist entry UUID.
     * Delete Watchlist Entry
     */
    async deleteWatchlistEntry(requestParameters: DeleteWatchlistEntryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountMutationResponse> {
        const response = await this.deleteWatchlistEntryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listWatchlist without sending the request
     */
    async listWatchlistRequestOpts(requestParameters: ListWatchlistRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        if (requestParameters['search'] != null) {
            queryParameters['search'] = requestParameters['search'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/watchlist`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * List saved watchlist items for the authenticated user.  Behavior: - accepts either a real API key or a session JWT bearer token - offset pagination ordered by newest saved first - `search` matches exact numeric `item_id` or case-insensitive item-name substring - response pagination includes a real `total` count
     * List Watchlist
     */
    async listWatchlistRaw(requestParameters: ListWatchlistRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WatchlistResponse>> {
        const requestOptions = await this.listWatchlistRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WatchlistResponseFromJSON(jsonValue));
    }

    /**
     * List saved watchlist items for the authenticated user.  Behavior: - accepts either a real API key or a session JWT bearer token - offset pagination ordered by newest saved first - `search` matches exact numeric `item_id` or case-insensitive item-name substring - response pagination includes a real `total` count
     * List Watchlist
     */
    async listWatchlist(requestParameters: ListWatchlistRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WatchlistResponse> {
        const response = await this.listWatchlistRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
