/* tslint:disable */
/* eslint-disable */
/**
 * CS2Cap
 * CS2Cap aggregates and analyzes Counter-Strike 2 marketplace data across multiple providers via a unified REST API.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AccountMutationResponse,
  AlertBatchCreateRequest,
  AlertBatchCreateResponse,
  AlertCreateRequest,
  AlertDefinition,
  AlertEventsResponse,
  AlertListResponse,
  AlertUpdateRequest,
  ErrorResponse,
  ValidationErrorResponse,
} from '../models/index';
import {
    AccountMutationResponseFromJSON,
    AccountMutationResponseToJSON,
    AlertBatchCreateRequestFromJSON,
    AlertBatchCreateRequestToJSON,
    AlertBatchCreateResponseFromJSON,
    AlertBatchCreateResponseToJSON,
    AlertCreateRequestFromJSON,
    AlertCreateRequestToJSON,
    AlertDefinitionFromJSON,
    AlertDefinitionToJSON,
    AlertEventsResponseFromJSON,
    AlertEventsResponseToJSON,
    AlertListResponseFromJSON,
    AlertListResponseToJSON,
    AlertUpdateRequestFromJSON,
    AlertUpdateRequestToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    ValidationErrorResponseFromJSON,
    ValidationErrorResponseToJSON,
} from '../models/index';

export interface CreateAlertRequest {
    alertCreateRequest: AlertCreateRequest;
}

export interface CreateAlertsBatchRequest {
    alertBatchCreateRequest: AlertBatchCreateRequest;
}

export interface DeleteAlertRequest {
    alertId: string;
}

export interface ListAlertEventsRequest {
    limit?: number;
    offset?: number;
}

export interface ListAlertsRequest {
    limit?: number;
    offset?: number;
    search?: string | null;
}

export interface UpdateAlertRequest {
    alertId: string;
    alertUpdateRequest: AlertUpdateRequest;
}

/**
 * 
 */
export class AccountAlertsApi extends runtime.BaseAPI {

    /**
     * Creates request options for createAlert without sending the request
     */
    async createAlertRequestOpts(requestParameters: CreateAlertRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['alertCreateRequest'] == null) {
            throw new runtime.RequiredError(
                'alertCreateRequest',
                'Required parameter "alertCreateRequest" was null or undefined when calling createAlert().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/alerts`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AlertCreateRequestToJSON(requestParameters['alertCreateRequest']),
        };
    }

    /**
     * Create a new item-scoped account alert.  Supported kinds: - `price_below`: trigger when the current best ask is at or below the threshold - `price_above`: trigger when the current best ask is at or above the threshold - `spread_exceeds`: trigger when `((best_ask - best_bid) / best_ask) * 100` meets or exceeds the threshold  Rules: - accepts either a real API key or a session JWT bearer token - `spread_exceeds` ignores `threshold_currency` - price alerts default `threshold_currency` to the account preferred currency when omitted; accounts that have not set a preference use USD - enabled alerts require at least one configured delivery channel (verified email delivery or an active webhook destination) - the active enabled-alert cap is tier-controlled
     * Create Alert
     */
    async createAlertRaw(requestParameters: CreateAlertRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AlertDefinition>> {
        const requestOptions = await this.createAlertRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AlertDefinitionFromJSON(jsonValue));
    }

    /**
     * Create a new item-scoped account alert.  Supported kinds: - `price_below`: trigger when the current best ask is at or below the threshold - `price_above`: trigger when the current best ask is at or above the threshold - `spread_exceeds`: trigger when `((best_ask - best_bid) / best_ask) * 100` meets or exceeds the threshold  Rules: - accepts either a real API key or a session JWT bearer token - `spread_exceeds` ignores `threshold_currency` - price alerts default `threshold_currency` to the account preferred currency when omitted; accounts that have not set a preference use USD - enabled alerts require at least one configured delivery channel (verified email delivery or an active webhook destination) - the active enabled-alert cap is tier-controlled
     * Create Alert
     */
    async createAlert(requestParameters: CreateAlertRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AlertDefinition> {
        const response = await this.createAlertRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for createAlertsBatch without sending the request
     */
    async createAlertsBatchRequestOpts(requestParameters: CreateAlertsBatchRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['alertBatchCreateRequest'] == null) {
            throw new runtime.RequiredError(
                'alertBatchCreateRequest',
                'Required parameter "alertBatchCreateRequest" was null or undefined when calling createAlertsBatch().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/alerts/batch`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AlertBatchCreateRequestToJSON(requestParameters['alertBatchCreateRequest']),
        };
    }

    /**
     * Create multiple item-scoped account alerts in one request.  Rules: - accepts either a real API key or a session JWT bearer token - maximum 100 alerts per request - gated by the `batch_alert_creation_access` feature flag - enabled alerts are preflight-checked against the tier active-alert cap before inserts - exact duplicate alert payloads in the same request are rejected inline - the response preserves request order and reports per-item created/error status - the request consumes one API call regardless of batch size
     * Create Alerts Batch
     */
    async createAlertsBatchRaw(requestParameters: CreateAlertsBatchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AlertBatchCreateResponse>> {
        const requestOptions = await this.createAlertsBatchRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AlertBatchCreateResponseFromJSON(jsonValue));
    }

    /**
     * Create multiple item-scoped account alerts in one request.  Rules: - accepts either a real API key or a session JWT bearer token - maximum 100 alerts per request - gated by the `batch_alert_creation_access` feature flag - enabled alerts are preflight-checked against the tier active-alert cap before inserts - exact duplicate alert payloads in the same request are rejected inline - the response preserves request order and reports per-item created/error status - the request consumes one API call regardless of batch size
     * Create Alerts Batch
     */
    async createAlertsBatch(requestParameters: CreateAlertsBatchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AlertBatchCreateResponse> {
        const response = await this.createAlertsBatchRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteAlert without sending the request
     */
    async deleteAlertRequestOpts(requestParameters: DeleteAlertRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['alertId'] == null) {
            throw new runtime.RequiredError(
                'alertId',
                'Required parameter "alertId" was null or undefined when calling deleteAlert().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/alerts/{alert_id}`;
        urlPath = urlPath.replace(`{${"alert_id"}}`, encodeURIComponent(String(requestParameters['alertId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Delete one alert definition owned by the authenticated user.
     * Delete Alert
     */
    async deleteAlertRaw(requestParameters: DeleteAlertRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountMutationResponse>> {
        const requestOptions = await this.deleteAlertRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountMutationResponseFromJSON(jsonValue));
    }

    /**
     * Delete one alert definition owned by the authenticated user.
     * Delete Alert
     */
    async deleteAlert(requestParameters: DeleteAlertRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountMutationResponse> {
        const response = await this.deleteAlertRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listAlertEvents without sending the request
     */
    async listAlertEventsRequestOpts(requestParameters: ListAlertEventsRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/alerts/events`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * List recent alert trigger events and delivery attempts for the authenticated user.  Behavior: - accepts either a real API key or a session JWT bearer token - offset pagination ordered by newest event first - delivery rows currently reflect email delivery attempts
     * List Alert Events
     */
    async listAlertEventsRaw(requestParameters: ListAlertEventsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AlertEventsResponse>> {
        const requestOptions = await this.listAlertEventsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AlertEventsResponseFromJSON(jsonValue));
    }

    /**
     * List recent alert trigger events and delivery attempts for the authenticated user.  Behavior: - accepts either a real API key or a session JWT bearer token - offset pagination ordered by newest event first - delivery rows currently reflect email delivery attempts
     * List Alert Events
     */
    async listAlertEvents(requestParameters: ListAlertEventsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AlertEventsResponse> {
        const response = await this.listAlertEventsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listAlerts without sending the request
     */
    async listAlertsRequestOpts(requestParameters: ListAlertsRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        if (requestParameters['search'] != null) {
            queryParameters['search'] = requestParameters['search'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/alerts`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * List configured item alerts for the authenticated user.  Behavior: - accepts either a real API key or a session JWT bearer token - offset pagination ordered by newest created first - `search` matches exact numeric `item_id` or case-insensitive item-name substring - response pagination includes a real `total` count
     * List Alerts
     */
    async listAlertsRaw(requestParameters: ListAlertsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AlertListResponse>> {
        const requestOptions = await this.listAlertsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AlertListResponseFromJSON(jsonValue));
    }

    /**
     * List configured item alerts for the authenticated user.  Behavior: - accepts either a real API key or a session JWT bearer token - offset pagination ordered by newest created first - `search` matches exact numeric `item_id` or case-insensitive item-name substring - response pagination includes a real `total` count
     * List Alerts
     */
    async listAlerts(requestParameters: ListAlertsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AlertListResponse> {
        const response = await this.listAlertsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateAlert without sending the request
     */
    async updateAlertRequestOpts(requestParameters: UpdateAlertRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['alertId'] == null) {
            throw new runtime.RequiredError(
                'alertId',
                'Required parameter "alertId" was null or undefined when calling updateAlert().'
            );
        }

        if (requestParameters['alertUpdateRequest'] == null) {
            throw new runtime.RequiredError(
                'alertUpdateRequest',
                'Required parameter "alertUpdateRequest" was null or undefined when calling updateAlert().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/account/alerts/{alert_id}`;
        urlPath = urlPath.replace(`{${"alert_id"}}`, encodeURIComponent(String(requestParameters['alertId'])));

        return {
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: AlertUpdateRequestToJSON(requestParameters['alertUpdateRequest']),
        };
    }

    /**
     * Update threshold fields or enabled state for an existing alert.  At least one of `threshold_value`, `threshold_currency`, or `is_enabled` must be provided. Enabling an alert re-applies tier, configured-delivery-channel, and enabled-alert-limit checks.
     * Update Alert
     */
    async updateAlertRaw(requestParameters: UpdateAlertRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AlertDefinition>> {
        const requestOptions = await this.updateAlertRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AlertDefinitionFromJSON(jsonValue));
    }

    /**
     * Update threshold fields or enabled state for an existing alert.  At least one of `threshold_value`, `threshold_currency`, or `is_enabled` must be provided. Enabling an alert re-applies tier, configured-delivery-channel, and enabled-alert-limit checks.
     * Update Alert
     */
    async updateAlert(requestParameters: UpdateAlertRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AlertDefinition> {
        const response = await this.updateAlertRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
