# Webhook Lifecycle

Demonstrates webhook list, create, update, secret rotation, delivery history, delivery detail, and cleanup under `/v1/account/webhooks`.

```bash
npm run example -- examples/quant-tier/webhook-lifecycle/webhook_lifecycle.ts
npm run example -- examples/quant-tier/webhook-lifecycle/webhook_lifecycle.ts --keep-resources
```
